export interface IProfile {
  userId: string;
  displayName: string;
  email: string;
  photoURL: string;
  isAdmin: boolean;
  isEnabled: boolean;
  // countryCode: string;
  // timeZone: string;
}

